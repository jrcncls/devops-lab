---
- name: Update application repository
  become: true
  become_user: kube
  
  ansible.builtin.git:
    repo: "{{ app_repo }}"
    dest: "{{ app_directory }}"
    version: "{{ app_branch }}"
    update: true
    accept_hostkey: true